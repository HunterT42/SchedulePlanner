<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchedulePlannerApp.CompletedTasksPage"
             Title="Выполненные задачи">
    <VerticalStackLayout Padding="20">
        <!-- Заголовок -->
        <Label Text="Выполненные задачи" FontSize="24" HorizontalOptions="Center" />

        <!-- Список выполненных задач -->
        <ListView x:Name="CompletedTaskListView" Margin="0,20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="5"
                                  WidthRequest="372"
                                  HeightRequest="60"
                                  ColumnSpacing="30">
                            <!-- Информация о задаче -->
                            <StackLayout Grid.Column="0" VerticalOptions="CenterAndExpand">
                                <Label Text="{Binding Name}" FontSize="16" VerticalOptions="CenterAndExpand" />
                                <Label Text="Указанное время:" FontSize="12" TextColor="Gray" />
                                <Label Text="{Binding Time}" FontSize="14" TextColor="Gray" VerticalOptions="CenterAndExpand" />
                            </StackLayout>

                            <!-- Продолжительность выполнения -->
                            <StackLayout Grid.Column="1" VerticalOptions="CenterAndExpand" HorizontalOptions="Center">
                                <Label Text="Длительность:" FontSize="12" TextColor="Gray" />
                                <Label Text="{Binding Duration}" FontSize="14" TextColor="Blue" VerticalOptions="CenterAndExpand" />
                            </StackLayout>

                            <!-- Кнопка удаления -->
                            <ImageButton Grid.Column="2"
                                         Source="delete.png"
                                         CommandParameter="{Binding .}"
                                         Clicked="OnDeleteCompletedTaskClicked"
                                         HorizontalOptions="End"
                                         VerticalOptions="Center"
                                         WidthRequest="35"
                                         HeightRequest="35" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>
